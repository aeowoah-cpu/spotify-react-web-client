// LoginModal is no longer used — authentication is handled by the Auth page.
// Kept as a stub to avoid breaking any imports.
export const LoginModal = () => null;
LoginModal.displayName = 'LoginModal';
